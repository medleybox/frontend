#!/bin/sh

docker run \
  --rm \
  -it \
  -p 8080:8080 \
  --entrypoint="node" \
  -v $(pwd):$(pwd) \
  --workdir $(pwd) \
  node:16-alpine \
  ./node_modules/.bin/vue-cli-service serve --public="medleybox.local"